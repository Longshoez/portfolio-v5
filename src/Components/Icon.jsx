import React from 'react'

const Icon = () => {
    return (
        <div className="Icon">
            <h1>{props.count}</h1>
            <p>{props.label}</p>
        </div>
    )
}

export default Icon
